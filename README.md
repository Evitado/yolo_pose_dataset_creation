# create_yolo_pose_dataset
Export and augment an aircraft YOLO-Pose dataset from HDF5 files created from bag . Includes projection of 3D keypoints, ray-based visibility filtering, synthetic keypoint generation, dataset augmentation (roll + scale), and visualization tools
